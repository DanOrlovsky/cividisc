<div class="container">

    {{#if message}} 
        <h4 class="alert alert-danger">{{message}}</h4>
    {{/if}}

    <div class="col-md-5 login-page bg-light">
        <h2 class="font-weight-light">Please Login</h2>
        <p class="lead"></p>

        <form action='/login' method='POST'>

            {{!-- <div class="input-group">
                <span class="input-group-addon" id="basic-addon1">@</span>
                <input class="form-control" name="displayName" placeholder="Display Name" aria-label="Username" aria-describedby="basic-addon1" type='text' />
            </div>
            <br> --}}
            <div class="form-group">
                <input type="email" class="form-control" name="inputEmail" id="inputEmail" aria-describedby="inputEmailDesc" placeholder="Email Address">
            </div>
            <div class="form-group">
                <input type="password" class="form-control" name="inputPassword" id="inputPassword" placeholder="Password">
            </div>
            <button type="submit" class="btn btn-outline-success">Login</button>
            <button type="Cancel" class="btn btn-outline-secondary">Cancel</button>
        </form>
        <p></p>
        <p>
            Don't have an account? <a href="/signup">Sign up today!</a>
        </p>
    </div>
</div>