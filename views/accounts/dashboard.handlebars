<h2>User's Dashboard!</h2>

<div class="col-md-8 login-page">
    {{#if postMessage}}
        <h2 class="alert alert-success">{{ postMessage}}</h2>
    {{/if}}

    Welcome back, {{ user.email }}!

</div>
