

<div class="container">


    <h2>{{ user.displayName}}'s Dashboard!</h2>
    <h2>Name: {{user.firstName}} {{user.lastName}}</h2>
    <h2>Reputation: {{user.rep}} </h2>

</div>

    <div class="container" >
    {{#each posts as |post|}}
    {{> post post=post}}
    {{/each}}
    </div>

