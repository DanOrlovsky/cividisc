
<div class="container">
 
        {{#unless posts}}
            <h2>Nothing to show</h2>
        {{/unless}}
        {{#if posts}}
            {{#each posts}}
                {{> post}}
            {{/each}}
        {{/if}}

</div>

<script src="../assets/js/index.js"></script>

