
<div class="container">
    {{#unless posts}}
        <h2>Nothing to show</h2>
    {{/unless}}
    {{#if posts}}
        {{#each posts as |post|}}
            {{> post post }}
        {{/each}}
    {{/if}}
</div>

